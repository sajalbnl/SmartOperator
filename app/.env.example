# HTTPS base URL of the deployed Express server, with no trailing slash.
# For a physical-device LAN test, use the computer's LAN IP rather than localhost.
EXPO_PUBLIC_API_URL=https://smartoperator.example.com
